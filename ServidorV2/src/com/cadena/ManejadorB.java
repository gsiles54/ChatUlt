package com.cadena;

import com.mensajes.Mensaje;

public class ManejadorB extends Manejador{

	@Override
	public void manejarPeticion(Mensaje peticion) {
		
	/*	if (peticion.getValue() >= 0) { 
			System.out.println("Losres mayores que 0 son manejados por "+this.getClass());
			System.out.println("Valorescion : "+ peticion.getDescripcion() + peticion.getValue());
		} else {
			siguiente.manejarPeticion(peticion);
		}
		*/
		
	}

}
